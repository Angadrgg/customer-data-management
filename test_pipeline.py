def test_dummy():
    # This is a simple dummy test so that Pytest doesn't fail the CI build
    # Pytest requires at least one test to be collected, otherwise it exits with an error code.
    assert True
